## Glob Patterns
- `**` traverse directories recursively
```nushell
# find all .txt files recursively
ls **/*.txt
```

redirect in nushell
```bash
"hello" > output.txt
```
```nushell
"hello" | save output.txt
```